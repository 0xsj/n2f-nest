/** Captured environment parsing with safe manifests and redacted secrets. */
export { Reader, type Var } from './reader.js';
export { map, type Lookup } from './lookup.js';
export { os } from './os.js';
